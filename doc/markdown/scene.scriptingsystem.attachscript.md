<!-- Do not edit this file. It is automatically generated by API Documenter. -->

[Home](doc/markdown/./index.md) &gt; [@zephyr3d/scene](doc/markdown/./scene.md) &gt; [ScriptingSystem](doc/markdown/./scene.scriptingsystem.md) &gt; [attachScript](doc/markdown/./scene.scriptingsystem.attachscript.md)

## ScriptingSystem.attachScript() method

Attaches a script to a host and returns the `RuntimeScript` instance.

Process: 1. Resolve module ID to a runtime URL via the registry. 2. Dynamically import the module. 3. Instantiate the default export if it is a constructor. 4. If this is the first time the instance is seen, call `onCreated()`<!-- -->. 5. Link the instance to the host, and call `onAttached(host)`<!-- -->. 6. Subscribe to the host's `'dispose'` event to auto-detach.

If the module cannot be resolved or does not export a default `RuntimeScript` subclass, a warning is logged and `null` is returned.

**Signature:**

```typescript
attachScript<T extends Host>(host: T, module: string): Promise<RuntimeScript<T>>;
```

## Parameters

<table><thead><tr><th>

Parameter


</th><th>

Type


</th><th>

Description


</th></tr></thead>
<tbody><tr><td>

host


</td><td>

T


</td><td>

The host object to attach the script to.


</td></tr>
<tr><td>

module


</td><td>

string


</td><td>

Module identifier used by the registry (logical ID or path).


</td></tr>
</tbody></table>

**Returns:**

Promise&lt;[RuntimeScript](doc/markdown/./scene.runtimescript.md)<!-- -->&lt;T&gt;&gt;

The instantiated `RuntimeScript<T>` or `null` on failure.

