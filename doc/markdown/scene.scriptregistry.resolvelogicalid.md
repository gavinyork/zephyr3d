<!-- Do not edit this file. It is automatically generated by API Documenter. -->

[Home](doc/markdown/./index.md) &gt; [@zephyr3d/scene](doc/markdown/./scene.md) &gt; [ScriptRegistry](doc/markdown/./scene.scriptregistry.md) &gt; [resolveLogicalId](doc/markdown/./scene.scriptregistry.resolvelogicalid.md)

## ScriptRegistry.resolveLogicalId() method

Resolves a specifier to a logical id suitable for further processing.

Resolution rules: - `#/path`<!-- -->: resolved against `scriptsRoot` via VFS join/normalize. - `./` or `../`<!-- -->: resolved relative to `fromId` directory (requires `fromId`<!-- -->). - `/absolute`<!-- -->: treated as absolute from root (normalized). - Bare module in editor mode: if `/deps.lock.json` exists and contains an entry, map to the dependency's `entry` path; otherwise return as-is. - Else (non-editor bare module): return `spec` unchanged (external).

**Signature:**

```typescript
resolveLogicalId(spec: string, fromId?: string): Promise<string>;
```

## Parameters

<table><thead><tr><th>

Parameter


</th><th>

Type


</th><th>

Description


</th></tr></thead>
<tbody><tr><td>

spec


</td><td>

string


</td><td>

Import specifier string.


</td></tr>
<tr><td>

fromId


</td><td>

string


</td><td>

_(Optional)_ Optional base logical id used for relative resolution.


</td></tr>
</tbody></table>

**Returns:**

Promise&lt;string&gt;

A normalized logical id or an external specifier string.

## Exceptions

If a relative import is provided without `fromId`<!-- -->.

