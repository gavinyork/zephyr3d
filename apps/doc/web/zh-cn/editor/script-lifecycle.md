# 脚本结构与生命周期

编辑器内置 TypeScript 脚本支持。  
每个脚本可挂载到 Scene 对象，实现交互或动画逻辑。  

核心生命周期回调：

构造函数：实例创建时调用
- **onCreate()**：初始化方法，支持异步
- **onAttached()**：附加到对象后调用
- **onUpdate()**：每帧更新调用
- **onDetached()**：解除附加时调用
- **onDestroy()**：实例销毁前调用

📄 *示例文件：foo.ts*

📸 *示意图：脚本编辑器界面截图*
